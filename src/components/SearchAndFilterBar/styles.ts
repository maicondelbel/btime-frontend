import { styled } from 'styled-components'

export const SearchAndFilterBarContainer = styled.div`
  box-shadow: 0 4px 6px 0 rgba(12, 0, 46, 0.06);
  padding: 0 1.5rem;
`

export const SearchAndFilterBarContainerWrapper = styled.div`
  display: flex;
  align-items: center;
  justify-content: end;
  height: 3rem;
  min-height: 3rem;
  max-width: 90rem;
  margin: 0 auto;
  gap: 1rem;
`
